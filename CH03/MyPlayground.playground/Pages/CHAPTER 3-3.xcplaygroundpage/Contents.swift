
// 변수 만들기
var score = 100     // score라는 변수를 만들고 100을 넣습니다.
score = 200         // score를 200으로 변경합니다. 
score = 100 + 200   // score를 100 + 200으로 변경합니다. 
score = score * 2   // score를 score의 2배로 변경합니다. 


// 상수 만들기
let myAge1 = 20      // myAge라는 상수를 만들고 20을 넣습니다.


// 상수와 변수의 차이
//let answer = 2000 * 0.1   // 무슨 계산인지 알기 어렵습니다.
let taxRate = 0.1           // 부가가치세 0.1에 taxRate라는 이름을 지정
let answer = 300 * taxRate  // 부가가치세를 구하는 계산임을 어느 정도 이해할 수 있습니다.


// 오류 : 상수이므로 값을 변경하지 말아주세요!
let myAge = 20              // myAge라는 상수를 만들고 20을 넣습니다.
//myAge  = 30               // 상수는 값을 변경할 수 없으므로 오류가 발생합니다.


// 예약어는 사용할 수 없음
//let class = 1               // 예약어를 사용하면 오류가 발생합니다.


// 캐멀 케이스 이름의 예 
import UIKit

let myName = "NAME"
let dataNo = 1
let loopCount = 0
let photoFrameName = "photo.jpg"
let boxHeight = 150
let fontColor = UIColor.red()
let highScore = 99999

